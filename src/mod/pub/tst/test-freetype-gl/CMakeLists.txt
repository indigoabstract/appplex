target_sources(app_plex_main
    PRIVATE
    PUBLIC
)
set(headers
	"${CMAKE_CURRENT_LIST_DIR}/mod-test-freetype-gl.hxx"
)
set(sources
	"${CMAKE_CURRENT_LIST_DIR}/mod-test-freetype-gl.cxx"
)
target_sources(app_plex_main
    PUBLIC
	${headers}
    PRIVATE
	${sources}
)
